name: Build and Push Docker Image

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  # --- 任务 1: 构建并推送到 Docker Hub (CI) ---
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Push
        run: |
          # 注意：这里假设你的 Dockerfile 在根目录
          docker build -t ${{ secrets.DOCKER_USERNAME }}/sentinel-monitor:latest .
          docker push ${{ secrets.DOCKER_USERNAME }}/sentinel-monitor:latest

  # --- 任务 2: 部署到服务器 (CD) ---
  deploy-to-k8s:
    needs: build-and-push # 必须等 CI 阶段成功了才执行
    runs-on: self-hosted  # 🌟【魔法核心】呼叫你虚拟机里的卧底！
    steps:
    - name: 检出代码 (卧底把带有 k8s-deploy.yaml 的代码拉到虚拟机里)
      uses: actions/checkout@v3

    - name: 部署到 K3s 集群！
      # 卧底代替你，敲下 kubectl apply 命令！
      run: |
        kubectl apply -f k8s-deploy.yaml
        kubectl rollout restart deployment my-go-app-deployment